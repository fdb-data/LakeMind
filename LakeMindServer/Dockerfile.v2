FROM lakemind/server-api:latest
WORKDIR /app
RUN pip install alembic sqlalchemy ulid-py
COPY pyproject.toml ./
COPY src ./src
COPY alembic.ini ./
COPY migrations ./migrations
RUN pip install --no-deps .
ENV LAKE_CONFIG=/etc/lakemind/engines.yaml
EXPOSE 10823
CMD ["python", "-m", "lakemind_server"]
